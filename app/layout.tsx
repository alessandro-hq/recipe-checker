import type { Metadata } from "next";
import "./globals.css";
import Navbar from "@/components/layout/Navbar";
import Footer from "@/components/layout/Footer";

export const metadata: Metadata = {
  title: "Recipe Checker",
  description: "Browse hundreds of recipes and discover what you can make with ingredients you already have.",
  openGraph: {
    title: "Recipe Checker",
    description: "Browse recipes and match them to your available ingredients.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
